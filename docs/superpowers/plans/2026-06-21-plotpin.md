# PlotPin Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 单人自用的网页工具:上传 CSV → 配置坐标轴/标题 → 生成折线图 → 得到永久不变的交互页链接与静态图片链接,可嵌入笔记。

**Architecture:** FastAPI + Jinja2 后端直出页面;交互图用本地打包的 Plotly.js,静态图用 matplotlib 按需渲染并永久缓存;CSV 数值采用工程计数法(SI 词头)解析与显示;元数据存 SQLite,CSV 与缓存图片存文件系统,全部落在映射到 NAS 的 `data/` 卷里。

**Tech Stack:** Python 3.12,FastAPI,Jinja2,pandas,matplotlib,Plotly.js(vendored),SQLite,pytest + httpx,Docker / docker-compose。

## Global Constraints

- Python 3.12;依赖:`fastapi`、`uvicorn[standard]`、`jinja2`、`python-multipart`、`pandas`、`matplotlib`;开发依赖:`pytest`、`httpx`。
- 工程计数法 SI 词头(**大小写敏感**):`f`=1e-15, `p`=1e-12, `n`=1e-9, `u`=1e-6, `m`=1e-3, (无)=1, `k`=1e3, `M`=1e6, `G`=1e9, `T`=1e12。
- 尾数幅值约束:`[1, 1000)`(含 1、不含 1000)或 `0`;支持前导负号,负号之外的幅值仍须满足该区间。
- 解析与显示分离:CSV 数据**始终**解析成浮点数;`x_eng`/`y_eng` 仅控制坐标轴刻度与悬停的显示风格(工程计数法 vs 普通数字)。交互图与静态图共用同一份格式化逻辑。
- 严格校验:任一单元格非法(非法词头、幅值超区间、空值/缺失)或文件级错误一律拒绝整个文件,不创建记录。
- 链接 ID:8–12 位不可猜测短随机串。
- 所有持久化数据落在 `data/`(`data/csv/`、`data/cache/`、`data/plotpin.db`),映射为 Docker 卷。
- 中文错误提示;遵循 TDD,每个任务结束即提交。

## File Structure

- `requirements.txt` / `requirements-dev.txt` — 运行/开发依赖。
- `app/__init__.py` — 包标记。
- `app/eng_notation.py` — 工程计数法解析与数字/刻度格式化:`parse_eng`、`format_eng`、`format_plain`、`nice_ticks`、`EngParseError`、`PREFIXES`。
- `app/ids.py` — `new_id`。
- `app/parsing.py` — CSV 编码识别、读取、校验:`decode_bytes`、`read_csv_bytes`、`ParsedCSV`、`CSVParseError`。
- `app/storage.py` — SQLite + 文件系统持久化:`Chart`、`Storage`。
- `app/rendering.py` — 图形构建:`render_static`(matplotlib PNG/SVG)、`build_plotly_spec`(交互图 spec dict)。两者均消费 `eng_notation` 的格式化逻辑。
- `app/main.py` — FastAPI 应用、路由、`get_storage` 依赖。
- `templates/index.html`、`templates/chart.html`、`templates/404.html` — 页面。
- `static/plotly.min.js`、`static/app.css`、`static/upload.js` — 前端静态资源。
- `tests/` — 各模块单测 + 路由集成测试。
- `Dockerfile`、`docker-compose.yml`、`.dockerignore` — 打包。

---

### Task 1: 项目脚手架与依赖

**Files:**
- Create: `requirements.txt`, `requirements-dev.txt`, `app/__init__.py`, `app/main.py`, `tests/__init__.py`, `tests/test_smoke.py`, `pytest.ini`

**Interfaces:**
- Consumes: 无。
- Produces: `app.main:app`(FastAPI 实例);`app.main:get_storage`(占位依赖,Task 5 起被覆盖);健康路由 `GET /healthz` 返回 `{"status":"ok"}`。

- [ ] **Step 1: 写依赖与配置文件**

`requirements.txt`:
```
fastapi
uvicorn[standard]
jinja2
python-multipart
pandas
matplotlib
```

`requirements-dev.txt`:
```
-r requirements.txt
pytest
httpx
```

`pytest.ini`:
```
[pytest]
testpaths = tests
```

`app/__init__.py`: 空文件。
`tests/__init__.py`: 空文件。

- [ ] **Step 2: 写失败测试**

`tests/test_smoke.py`:
```python
from fastapi.testclient import TestClient
from app.main import app


def test_healthz():
    client = TestClient(app)
    resp = client.get("/healthz")
    assert resp.status_code == 200
    assert resp.json() == {"status": "ok"}
```

- [ ] **Step 3: 运行测试确认失败**

先装依赖:`pip install -r requirements-dev.txt`
Run: `pytest tests/test_smoke.py -v`
Expected: FAIL(`ModuleNotFoundError: No module named 'app.main'`)

- [ ] **Step 4: 写最小实现**

`app/main.py`:
```python
from fastapi import FastAPI

app = FastAPI(title="PlotPin")


def get_storage():  # 占位,Task 5 起返回真正的 Storage,测试用依赖覆盖
    raise NotImplementedError


@app.get("/healthz")
def healthz():
    return {"status": "ok"}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `pytest tests/test_smoke.py -v`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add requirements.txt requirements-dev.txt pytest.ini app tests
git commit -m "chore: scaffold FastAPI app with healthcheck"
```

---

### Task 2: 工程计数法模块

**Files:**
- Create: `app/eng_notation.py`, `tests/test_eng_notation.py`

**Interfaces:**
- Consumes: 无。
- Produces:
  - `PREFIXES: dict[str, float]` — 词头到倍率。
  - `class EngParseError(ValueError)`。
  - `parse_eng(s: str) -> float` — 解析工程计数法字符串为浮点;非法抛 `EngParseError`。
  - `format_eng(value: float) -> str` — 浮点格式化为工程计数法字符串。
  - `format_plain(value: float) -> str` — 浮点格式化为普通数字字符串。
  - `nice_ticks(lo: float, hi: float, target: int = 6) -> list[float]` — 生成"漂亮"刻度位置。

- [ ] **Step 1: 写失败测试(parse_eng)**

`tests/test_eng_notation.py`:
```python
import pytest
from app.eng_notation import (
    parse_eng,
    format_eng,
    format_plain,
    nice_ticks,
    EngParseError,
)


@pytest.mark.parametrize("text,expected", [
    ("47k", 47000.0),
    ("100m", 0.1),
    ("3.3", 3.3),
    ("0", 0.0),
    ("1k", 1000.0),
    ("999.9", 999.9),
    ("1", 1.0),
    ("-47k", -47000.0),
    ("470u", 470e-6),
    ("2.2M", 2.2e6),
    (" 47k ", 47000.0),
])
def test_parse_eng_valid(text, expected):
    assert parse_eng(text) == pytest.approx(expected)


@pytest.mark.parametrize("text", [
    "47x",      # 非法词头
    "4700",     # 无词头但 >=1000
    "1000",     # 边界,>=1000 非法
    "0.5",      # <1 无词头
    "",         # 空值
    "   ",      # 仅空白
    "47 k",     # 中间空格
    "abc",      # 非数字
])
def test_parse_eng_invalid(text):
    with pytest.raises(EngParseError):
        parse_eng(text)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_eng_notation.py -v`
Expected: FAIL(`ImportError`)

- [ ] **Step 3: 写最小实现(parse_eng)**

`app/eng_notation.py`:
```python
import math
import re

PREFIXES: dict[str, float] = {
    "f": 1e-15, "p": 1e-12, "n": 1e-9, "u": 1e-6, "m": 1e-3,
    "k": 1e3, "M": 1e6, "G": 1e9, "T": 1e12,
}
_EXP_TO_PREFIX = {
    -15: "f", -12: "p", -9: "n", -6: "u", -3: "m", 0: "",
    3: "k", 6: "M", 9: "G", 12: "T",
}
_NUM_RE = re.compile(r"^([+-]?)(\d+(?:\.\d+)?)([fpnumkMGT]?)$")


class EngParseError(ValueError):
    pass


def parse_eng(s: str) -> float:
    if s is None:
        raise EngParseError("空值")
    text = s.strip()
    if text == "":
        raise EngParseError("空值")
    match = _NUM_RE.match(text)
    if not match:
        raise EngParseError(f"`{s}` 无法解析")
    sign, mantissa_str, prefix = match.groups()
    mantissa = float(mantissa_str)
    if mantissa == 0:
        return 0.0
    if not (1.0 <= mantissa < 1000.0):
        raise EngParseError(f"`{s}` 的数值部分必须落在 [1, 1000) 或为 0")
    factor = PREFIXES[prefix] if prefix else 1.0
    value = mantissa * factor
    return -value if sign == "-" else value
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_eng_notation.py -v`
Expected: PASS

- [ ] **Step 5: 写失败测试(format_eng / format_plain)**

追加到 `tests/test_eng_notation.py`:
```python
@pytest.mark.parametrize("value,expected", [
    (47000.0, "47k"),
    (0.1, "100m"),
    (3.3, "3.3"),
    (0.0, "0"),
    (1000.0, "1k"),
    (-47000.0, "-47k"),
    (470e-6, "470u"),
    (2.2e6, "2.2M"),
])
def test_format_eng(value, expected):
    assert format_eng(value) == expected


@pytest.mark.parametrize("value,expected", [
    (47000.0, "47000"),
    (0.1, "0.1"),
    (3.3, "3.3"),
    (0.0, "0"),
])
def test_format_plain(value, expected):
    assert format_plain(value) == expected
```

- [ ] **Step 6: 运行测试确认失败**

Run: `pytest tests/test_eng_notation.py -k "format" -v`
Expected: FAIL(`NameError`/`AttributeError`:函数未定义)

- [ ] **Step 7: 实现 format_eng / format_plain**

追加到 `app/eng_notation.py`:
```python
def _trim(value: float) -> str:
    text = f"{value:.6g}"
    return text


def format_plain(value: float) -> str:
    if value == 0:
        return "0"
    return _trim(value)


def format_eng(value: float) -> str:
    if value == 0:
        return "0"
    negative = value < 0
    magnitude = abs(value)
    exp3 = int(math.floor(math.log10(magnitude) / 3.0)) * 3
    exp3 = max(-15, min(12, exp3))
    mantissa = magnitude / (10.0 ** exp3)
    prefix = _EXP_TO_PREFIX[exp3]
    text = _trim(mantissa)
    return ("-" if negative else "") + text + prefix
```

- [ ] **Step 8: 运行测试确认通过**

Run: `pytest tests/test_eng_notation.py -k "format" -v`
Expected: PASS

- [ ] **Step 9: 写失败测试(nice_ticks)**

追加到 `tests/test_eng_notation.py`:
```python
def test_nice_ticks_basic():
    ticks = nice_ticks(0.0, 100.0, target=6)
    assert ticks[0] <= 0.0
    assert ticks[-1] >= 100.0
    assert all(ticks[i] < ticks[i + 1] for i in range(len(ticks) - 1))
    assert 5 <= len(ticks) <= 9


def test_nice_ticks_equal_bounds():
    ticks = nice_ticks(5.0, 5.0)
    assert len(ticks) >= 2
```

- [ ] **Step 10: 运行测试确认失败**

Run: `pytest tests/test_eng_notation.py -k "nice_ticks" -v`
Expected: FAIL(`NameError`)

- [ ] **Step 11: 实现 nice_ticks**

追加到 `app/eng_notation.py`:
```python
def nice_ticks(lo: float, hi: float, target: int = 6) -> list[float]:
    if lo == hi:
        lo, hi = lo - 1.0, hi + 1.0
    if lo > hi:
        lo, hi = hi, lo
    span = hi - lo
    raw_step = span / max(1, target)
    mag = 10.0 ** math.floor(math.log10(raw_step))
    norm = raw_step / mag
    if norm < 1.5:
        step = 1.0
    elif norm < 3.0:
        step = 2.0
    elif norm < 7.0:
        step = 5.0
    else:
        step = 10.0
    step *= mag
    start = math.ceil(lo / step) * step
    ticks: list[float] = []
    value = start
    while value <= hi + step * 1e-9:
        ticks.append(round(value, 12))
        value += step
    return ticks
```

- [ ] **Step 12: 运行全部测试确认通过**

Run: `pytest tests/test_eng_notation.py -v`
Expected: PASS(全部)

- [ ] **Step 13: 提交**

```bash
git add app/eng_notation.py tests/test_eng_notation.py
git commit -m "feat: engineering notation parse/format and nice ticks"
```

---

### Task 3: 短随机 ID

**Files:**
- Create: `app/ids.py`, `tests/test_ids.py`

**Interfaces:**
- Consumes: 无。
- Produces: `new_id(length: int = 10) -> str` — 由 `[a-zA-Z0-9]` 组成的随机串,长度默认 10(在 8–12 区间内)。

- [ ] **Step 1: 写失败测试**

`tests/test_ids.py`:
```python
from app.ids import new_id


def test_new_id_default_length():
    assert len(new_id()) == 10


def test_new_id_charset():
    ident = new_id()
    assert ident.isalnum()
    assert ident.isascii()


def test_new_id_unique():
    ids = {new_id() for _ in range(1000)}
    assert len(ids) == 1000
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_ids.py -v`
Expected: FAIL(`ModuleNotFoundError`)

- [ ] **Step 3: 写最小实现**

`app/ids.py`:
```python
import secrets
import string

_ALPHABET = string.ascii_letters + string.digits


def new_id(length: int = 10) -> str:
    return "".join(secrets.choice(_ALPHABET) for _ in range(length))
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_ids.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add app/ids.py tests/test_ids.py
git commit -m "feat: short random id generator"
```

---

### Task 4: CSV 解析与校验

**Files:**
- Create: `app/parsing.py`, `tests/test_parsing.py`

**Interfaces:**
- Consumes: `app.eng_notation.parse_eng`、`EngParseError`。
- Produces:
  - `class CSVParseError(Exception)` — `str(err)` 为中文提示。
  - `@dataclass ParsedCSV` 字段:`x_label: str`、`x: list[float]`、`y_labels: list[str]`、`ys: list[list[float]]`(每个 Y 列一条 `list[float]`,与 `x` 等长)。
  - `decode_bytes(raw: bytes) -> str` — 优先 UTF-8,失败兜底 GBK,均失败抛 `CSVParseError`。
  - `read_csv_bytes(raw: bytes) -> ParsedCSV` — 解码 → pandas 读取 → 逐格按工程计数法解析校验。

- [ ] **Step 1: 写失败测试**

`tests/test_parsing.py`:
```python
import pytest
from app.parsing import read_csv_bytes, decode_bytes, ParsedCSV, CSVParseError


def _csv(text: str, encoding: str = "utf-8") -> bytes:
    return text.encode(encoding)


def test_read_valid_multi_y():
    raw = _csv("freq,gain,phase\n1k,3.3,100m\n2k,6.6,200m\n")
    parsed = read_csv_bytes(raw)
    assert isinstance(parsed, ParsedCSV)
    assert parsed.x_label == "freq"
    assert parsed.x == pytest.approx([1000.0, 2000.0])
    assert parsed.y_labels == ["gain", "phase"]
    assert parsed.ys[0] == pytest.approx([3.3, 6.6])
    assert parsed.ys[1] == pytest.approx([0.1, 0.2])


def test_decode_gbk_fallback():
    raw = "频率,增益\n1k,3.3\n".encode("gbk")
    text = decode_bytes(raw)
    assert "频率" in text


def test_reject_single_column():
    raw = _csv("only\n1k\n2k\n")
    with pytest.raises(CSVParseError):
        read_csv_bytes(raw)


def test_reject_bad_cell_reports_location():
    raw = _csv("x,y\n1k,3.3\n2k,47x\n")
    with pytest.raises(CSVParseError) as exc:
        read_csv_bytes(raw)
    msg = str(exc.value)
    assert "47x" in msg


def test_reject_empty_cell():
    raw = _csv("x,y\n1k,3.3\n2k,\n")
    with pytest.raises(CSVParseError):
        read_csv_bytes(raw)


def test_reject_not_csv():
    with pytest.raises(CSVParseError):
        read_csv_bytes(b"\x00\x01\x02 not text at all")
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_parsing.py -v`
Expected: FAIL(`ModuleNotFoundError`)

- [ ] **Step 3: 写最小实现**

`app/parsing.py`:
```python
import io
from dataclasses import dataclass

import pandas as pd

from app.eng_notation import EngParseError, parse_eng


class CSVParseError(Exception):
    pass


@dataclass
class ParsedCSV:
    x_label: str
    x: list[float]
    y_labels: list[str]
    ys: list[list[float]]


def decode_bytes(raw: bytes) -> str:
    for encoding in ("utf-8", "gbk"):
        try:
            return raw.decode(encoding)
        except UnicodeDecodeError:
            continue
    raise CSVParseError("无法识别文件编码(已尝试 UTF-8、GBK)")


def read_csv_bytes(raw: bytes) -> ParsedCSV:
    text = decode_bytes(raw)
    try:
        frame = pd.read_csv(io.StringIO(text), dtype=str)
    except Exception:
        raise CSVParseError("文件无法识别为 CSV")
    if frame.shape[1] < 2:
        raise CSVParseError("至少需要 X 列和 1 个 Y 列")
    if frame.shape[0] < 1:
        raise CSVParseError("CSV 没有数据行")
    columns = [str(c) for c in frame.columns]
    if len(set(columns)) != len(columns):
        raise CSVParseError("表头存在重复列名")

    def parse_cell(value, row_idx: int, col_idx: int) -> float:
        cell = "" if value is None else str(value)
        try:
            return parse_eng(cell)
        except EngParseError as err:
            raise CSVParseError(
                f"第 {row_idx + 2} 行 第 {col_idx + 1} 列 `{cell}` 无法解析:{err}"
            )

    x_values: list[float] = []
    for r, raw_cell in enumerate(frame.iloc[:, 0].tolist()):
        x_values.append(parse_cell(raw_cell, r, 0))

    y_labels = columns[1:]
    y_series: list[list[float]] = []
    for c in range(1, frame.shape[1]):
        col_values: list[float] = []
        for r, raw_cell in enumerate(frame.iloc[:, c].tolist()):
            col_values.append(parse_cell(raw_cell, r, c))
        y_series.append(col_values)

    return ParsedCSV(
        x_label=columns[0],
        x=x_values,
        y_labels=y_labels,
        ys=y_series,
    )
```

注意:`row_idx + 2` 是因为表头占第 1 行、pandas 数据行从 0 计;对外报第几行用人类可读的 1 基行号(含表头)。pandas 把空单元格读成 `NaN`(float),`str(NaN)` => `"nan"`,`parse_eng("nan")` 会因正则不匹配而报错 → 满足"空值视为错误"。

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_parsing.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add app/parsing.py tests/test_parsing.py
git commit -m "feat: CSV decoding, parsing and strict validation"
```

---

### Task 5: 持久化(SQLite + 文件系统)

**Files:**
- Create: `app/storage.py`, `tests/test_storage.py`

**Interfaces:**
- Consumes: 无。
- Produces:
  - `@dataclass Chart` 字段:`id: str`、`title: str`、`x_title: str`、`y_title: str`、`x_eng: bool`、`y_eng: bool`、`created_at: str`。
  - `class Storage`:
    - `__init__(self, data_dir: str | Path)` — 建好 `csv/`、`cache/` 目录与库表。
    - `exists(self, chart_id: str) -> bool`。
    - `save_chart(self, chart_id, title, x_title, y_title, x_eng, y_eng, csv_bytes: bytes) -> Chart`。
    - `get_chart(self, chart_id: str) -> Chart | None`。
    - `list_charts(self) -> list[Chart]` — 按 `created_at` 倒序。
    - `read_csv(self, chart_id: str) -> bytes`。
    - `csv_path(self, chart_id: str) -> Path`、`cache_path(self, chart_id: str, ext: str) -> Path`。

- [ ] **Step 1: 写失败测试**

`tests/test_storage.py`:
```python
from app.storage import Storage, Chart


def test_save_and_get(tmp_path):
    store = Storage(tmp_path)
    chart = store.save_chart(
        "abc123xyz0", "标题", "X", "Y", True, False, b"x,y\n1k,3.3\n"
    )
    assert isinstance(chart, Chart)
    assert chart.id == "abc123xyz0"
    assert chart.x_eng is True
    assert chart.y_eng is False

    fetched = store.get_chart("abc123xyz0")
    assert fetched is not None
    assert fetched.title == "标题"
    assert store.read_csv("abc123xyz0") == b"x,y\n1k,3.3\n"


def test_get_missing_returns_none(tmp_path):
    store = Storage(tmp_path)
    assert store.get_chart("nope") is None


def test_exists(tmp_path):
    store = Storage(tmp_path)
    assert store.exists("abc") is False
    store.save_chart("abc", "t", "x", "y", True, True, b"x,y\n1k,3.3\n")
    assert store.exists("abc") is True


def test_list_charts_desc(tmp_path):
    store = Storage(tmp_path)
    store.save_chart("id1", "first", "x", "y", True, True, b"x,y\n1k,3.3\n")
    store.save_chart("id2", "second", "x", "y", True, True, b"x,y\n1k,3.3\n")
    charts = store.list_charts()
    assert [c.id for c in charts] == ["id2", "id1"]


def test_persistence_across_instances(tmp_path):
    Storage(tmp_path).save_chart("keep", "t", "x", "y", True, True, b"x,y\n1k,3.3\n")
    assert Storage(tmp_path).get_chart("keep") is not None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_storage.py -v`
Expected: FAIL(`ModuleNotFoundError`)

- [ ] **Step 3: 写最小实现**

`app/storage.py`:
```python
import sqlite3
from dataclasses import dataclass
from datetime import datetime, timezone
from pathlib import Path


@dataclass
class Chart:
    id: str
    title: str
    x_title: str
    y_title: str
    x_eng: bool
    y_eng: bool
    created_at: str


class Storage:
    def __init__(self, data_dir):
        self.data_dir = Path(data_dir)
        self.csv_dir = self.data_dir / "csv"
        self.cache_dir = self.data_dir / "cache"
        self.db_path = self.data_dir / "plotpin.db"
        self.csv_dir.mkdir(parents=True, exist_ok=True)
        self.cache_dir.mkdir(parents=True, exist_ok=True)
        self._init_db()

    def _connect(self) -> sqlite3.Connection:
        conn = sqlite3.connect(self.db_path)
        conn.row_factory = sqlite3.Row
        return conn

    def _init_db(self) -> None:
        with self._connect() as conn:
            conn.execute(
                """
                CREATE TABLE IF NOT EXISTS charts (
                    id TEXT PRIMARY KEY,
                    title TEXT NOT NULL,
                    x_title TEXT NOT NULL,
                    y_title TEXT NOT NULL,
                    x_eng INTEGER NOT NULL,
                    y_eng INTEGER NOT NULL,
                    created_at TEXT NOT NULL
                )
                """
            )

    def csv_path(self, chart_id: str) -> Path:
        return self.csv_dir / f"{chart_id}.csv"

    def cache_path(self, chart_id: str, ext: str) -> Path:
        return self.cache_dir / f"{chart_id}.{ext}"

    def exists(self, chart_id: str) -> bool:
        with self._connect() as conn:
            row = conn.execute(
                "SELECT 1 FROM charts WHERE id = ?", (chart_id,)
            ).fetchone()
            return row is not None

    def save_chart(self, chart_id, title, x_title, y_title, x_eng, y_eng, csv_bytes) -> Chart:
        created_at = datetime.now(timezone.utc).isoformat()
        self.csv_path(chart_id).write_bytes(csv_bytes)
        with self._connect() as conn:
            conn.execute(
                """
                INSERT INTO charts
                    (id, title, x_title, y_title, x_eng, y_eng, created_at)
                VALUES (?, ?, ?, ?, ?, ?, ?)
                """,
                (chart_id, title, x_title, y_title, int(x_eng), int(y_eng), created_at),
            )
        return Chart(chart_id, title, x_title, y_title, bool(x_eng), bool(y_eng), created_at)

    def _row_to_chart(self, row: sqlite3.Row) -> Chart:
        return Chart(
            id=row["id"],
            title=row["title"],
            x_title=row["x_title"],
            y_title=row["y_title"],
            x_eng=bool(row["x_eng"]),
            y_eng=bool(row["y_eng"]),
            created_at=row["created_at"],
        )

    def get_chart(self, chart_id: str) -> Chart | None:
        with self._connect() as conn:
            row = conn.execute(
                "SELECT * FROM charts WHERE id = ?", (chart_id,)
            ).fetchone()
            return self._row_to_chart(row) if row else None

    def list_charts(self) -> list[Chart]:
        with self._connect() as conn:
            rows = conn.execute(
                "SELECT * FROM charts ORDER BY created_at DESC, id DESC"
            ).fetchall()
            return [self._row_to_chart(r) for r in rows]

    def read_csv(self, chart_id: str) -> bytes:
        return self.csv_path(chart_id).read_bytes()
```

注意:`list_charts` 排序加 `id DESC` 兜底,避免同一时间戳(测试里两条记录可能落在同一微秒)导致顺序不稳。

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_storage.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add app/storage.py tests/test_storage.py
git commit -m "feat: SQLite + filesystem persistence layer"
```

---

### Task 6: 图形渲染(静态 + 交互 spec)

**Files:**
- Create: `app/rendering.py`, `tests/test_rendering.py`

**Interfaces:**
- Consumes: `app.parsing.ParsedCSV`;`app.eng_notation.format_eng`、`format_plain`、`nice_ticks`。
- Produces:
  - `render_static(parsed: ParsedCSV, title: str, x_title: str, y_title: str, x_eng: bool, y_eng: bool, fmt: str) -> bytes` — `fmt` 取 `"png"` 或 `"svg"`,返回图片字节。
  - `build_plotly_spec(parsed: ParsedCSV, title: str, x_title: str, y_title: str, x_eng: bool, y_eng: bool) -> dict` — 返回含 `"data"` 与 `"layout"` 两键的 dict,供 Plotly.js 使用。

- [ ] **Step 1: 写失败测试**

`tests/test_rendering.py`:
```python
from app.parsing import ParsedCSV
from app.rendering import render_static, build_plotly_spec


def _sample() -> ParsedCSV:
    return ParsedCSV(
        x_label="freq",
        x=[1000.0, 2000.0, 3000.0],
        y_labels=["gain", "phase"],
        ys=[[3.3, 6.6, 9.9], [0.1, 0.2, 0.3]],
    )


def test_render_png_returns_png_bytes():
    data = render_static(_sample(), "T", "X", "Y", True, True, "png")
    assert data[:8] == b"\x89PNG\r\n\x1a\n"


def test_render_svg_returns_svg():
    data = render_static(_sample(), "T", "X", "Y", True, True, "svg")
    assert b"<svg" in data[:512]


def test_build_plotly_spec_structure():
    spec = build_plotly_spec(_sample(), "T", "X", "Y", True, False)
    assert len(spec["data"]) == 2
    assert spec["data"][0]["name"] == "gain"
    assert spec["data"][0]["x"] == [1000.0, 2000.0, 3000.0]
    assert spec["layout"]["title"]["text"] == "T"
    # x 轴工程计数法显示:刻度文本含 SI 词头
    assert any("k" in t for t in spec["layout"]["xaxis"]["ticktext"])
    # y 轴关闭工程计数法:刻度文本为普通数字(无字母后缀)
    assert all(not any(ch.isalpha() for ch in t)
               for t in spec["layout"]["yaxis"]["ticktext"])
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_rendering.py -v`
Expected: FAIL(`ModuleNotFoundError`)

- [ ] **Step 3: 写最小实现**

`app/rendering.py`:
```python
import io

import matplotlib

matplotlib.use("Agg")  # 无头环境,必须在 pyplot 之前设置

import matplotlib.pyplot as plt  # noqa: E402
from matplotlib.ticker import FuncFormatter  # noqa: E402

from app.eng_notation import format_eng, format_plain, nice_ticks  # noqa: E402
from app.parsing import ParsedCSV  # noqa: E402


def _formatter(use_eng: bool):
    return format_eng if use_eng else format_plain


def render_static(parsed, title, x_title, y_title, x_eng, y_eng, fmt):
    fig, ax = plt.subplots(figsize=(8, 5))
    try:
        for label, ys in zip(parsed.y_labels, parsed.ys):
            ax.plot(parsed.x, ys, label=label)
        ax.set_title(title)
        ax.set_xlabel(x_title)
        ax.set_ylabel(y_title)
        ax.legend()
        xf = _formatter(x_eng)
        yf = _formatter(y_eng)
        ax.xaxis.set_major_formatter(FuncFormatter(lambda v, _pos: xf(v)))
        ax.yaxis.set_major_formatter(FuncFormatter(lambda v, _pos: yf(v)))
        buf = io.BytesIO()
        fig.savefig(buf, format=fmt, bbox_inches="tight")
        return buf.getvalue()
    finally:
        plt.close(fig)


def build_plotly_spec(parsed, title, x_title, y_title, x_eng, y_eng):
    xf = _formatter(x_eng)
    yf = _formatter(y_eng)

    traces = []
    for label, ys in zip(parsed.y_labels, parsed.ys):
        traces.append({
            "type": "scatter",
            "mode": "lines",
            "name": label,
            "x": parsed.x,
            "y": ys,
            "customdata": [[xf(xv), yf(yv)] for xv, yv in zip(parsed.x, ys)],
            "hovertemplate": (
                f"{x_title}: %{{customdata[0]}}<br>"
                f"{y_title}: %{{customdata[1]}}<extra>%{{fullData.name}}</extra>"
            ),
        })

    x_ticks = nice_ticks(min(parsed.x), max(parsed.x))
    all_y = [v for ys in parsed.ys for v in ys]
    y_ticks = nice_ticks(min(all_y), max(all_y))

    layout = {
        "title": {"text": title},
        "xaxis": {
            "title": {"text": x_title},
            "tickvals": x_ticks,
            "ticktext": [xf(t) for t in x_ticks],
        },
        "yaxis": {
            "title": {"text": y_title},
            "tickvals": y_ticks,
            "ticktext": [yf(t) for t in y_ticks],
        },
    }
    return {"data": traces, "layout": layout}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_rendering.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add app/rendering.py tests/test_rendering.py
git commit -m "feat: matplotlib static render and plotly spec builder"
```

---

### Task 7: 路由、模板与前端资源

**Files:**
- Modify: `app/main.py`
- Create: `templates/index.html`, `templates/chart.html`, `templates/404.html`, `static/app.css`, `static/upload.js`, `static/plotly.min.js`(下载), `tests/test_routes.py`

> **前端视觉:** 本任务的模板/CSS 涉及 UI 视觉。若 `frontend-design` skill 已启用(claude-plugins-official 插件),应先调用它来设计 `index.html`/`chart.html` 的视觉,再据此替换下方占位模板与 `app.css`;未启用则按其原则手工实现,不阻塞进度。计划中的模板/CSS 为功能占位,满足测试即可。

**Interfaces:**
- Consumes: `app.storage.Storage`、`app.parsing.read_csv_bytes`/`CSVParseError`、`app.rendering.render_static`/`build_plotly_spec`、`app.ids.new_id`、`app.eng_notation`。
- Produces(路由):
  - `GET /` — 上传区 + 图表清单(倒序)。
  - `POST /charts` — 表单 `file`(UploadFile)、`title`、`x_title`、`y_title`、`x_eng`(可选 checkbox)、`y_eng`(可选 checkbox);成功 303 跳转 `/chart/{id}`,失败 400 重渲染首页带中文错误。
  - `GET /chart/{id}` — 交互页;不存在返回 404。
  - `GET /chart/{id}.png` 与 `GET /chart/{id}.svg` — 按需渲染 + 缓存。
  - `get_storage()` 返回单例 `Storage`,数据目录取环境变量 `PLOTPIN_DATA_DIR`(默认 `data`)。

- [ ] **Step 1: 下载 Plotly.js 到本地静态资源**

Run:
```bash
mkdir -p static templates
curl -fsSL https://cdn.plot.ly/plotly-2.35.2.min.js -o static/plotly.min.js
test -s static/plotly.min.js && echo OK
```
Expected: 打印 `OK`(文件非空)。若构建机无外网,改为从有网机器拷贝该文件到 `static/plotly.min.js`;测试不依赖此文件。

- [ ] **Step 2: 写失败测试**

`tests/test_routes.py`:
```python
import pytest
from fastapi.testclient import TestClient

from app.main import app, get_storage
from app.storage import Storage


@pytest.fixture()
def client(tmp_path):
    store = Storage(tmp_path)
    app.dependency_overrides[get_storage] = lambda: store
    yield TestClient(app)
    app.dependency_overrides.clear()


def _upload(client, csv_text):
    return client.post(
        "/charts",
        data={"title": "T", "x_title": "X", "y_title": "Y",
              "x_eng": "on", "y_eng": "on"},
        files={"file": ("data.csv", csv_text.encode("utf-8"), "text/csv")},
        follow_redirects=False,
    )


def test_index_empty(client):
    resp = client.get("/")
    assert resp.status_code == 200


def test_upload_success_redirects(client):
    resp = _upload(client, "x,y\n1k,3.3\n2k,6.6\n")
    assert resp.status_code == 303
    assert resp.headers["location"].startswith("/chart/")


def test_upload_bad_cell_shows_error(client):
    resp = client.post(
        "/charts",
        data={"title": "T", "x_title": "X", "y_title": "Y"},
        files={"file": ("data.csv", b"x,y\n1k,47x\n", "text/csv")},
        follow_redirects=False,
    )
    assert resp.status_code == 400
    assert "47x" in resp.text


def test_chart_page_and_listing(client):
    loc = _upload(client, "x,y\n1k,3.3\n2k,6.6\n").headers["location"]
    chart_id = loc.rsplit("/", 1)[-1]
    page = client.get(f"/chart/{chart_id}")
    assert page.status_code == 200
    assert chart_id in client.get("/").text


def test_chart_not_found(client):
    assert client.get("/chart/missing").status_code == 404


def test_png_render_and_cache(client, tmp_path):
    chart_id = _upload(client, "x,y\n1k,3.3\n2k,6.6\n").headers["location"].rsplit("/", 1)[-1]
    resp = client.get(f"/chart/{chart_id}.png")
    assert resp.status_code == 200
    assert resp.headers["content-type"] == "image/png"
    assert resp.content[:8] == b"\x89PNG\r\n\x1a\n"
    # 第二次请求应命中缓存文件
    cached = tmp_path / "cache" / f"{chart_id}.png"
    assert cached.exists()


def test_svg_render(client):
    chart_id = _upload(client, "x,y\n1k,3.3\n2k,6.6\n").headers["location"].rsplit("/", 1)[-1]
    resp = client.get(f"/chart/{chart_id}.svg")
    assert resp.status_code == 200
    assert "image/svg" in resp.headers["content-type"]
```

- [ ] **Step 3: 运行测试确认失败**

Run: `pytest tests/test_routes.py -v`
Expected: FAIL(路由/模板未实现)

- [ ] **Step 4: 写模板**

`templates/index.html`:
```html
<!doctype html>
<html lang="zh">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>PlotPin</title>
  <link rel="stylesheet" href="/static/app.css">
</head>
<body>
  <h1>PlotPin</h1>
  {% if error %}<p class="error">{{ error }}</p>{% endif %}

  <form id="upload-form" action="/charts" method="post" enctype="multipart/form-data">
    <input type="file" name="file" id="file" accept=".csv" required>
    <div id="config" hidden>
      <label>图表标题 <input type="text" name="title" required></label>
      <label>X 轴标题 <input type="text" name="x_title" required></label>
      <label>Y 轴标题 <input type="text" name="y_title" required></label>
      <label><input type="checkbox" name="x_eng" checked> X 轴使用工程计数法</label>
      <label><input type="checkbox" name="y_eng" checked> Y 轴使用工程计数法</label>
      <button type="submit">生成图表</button>
    </div>
  </form>

  <h2>历史图表</h2>
  <ul class="chart-list">
    {% for c in charts %}
      <li><a href="/chart/{{ c.id }}">{{ c.title }}</a> <small>{{ c.created_at }}</small></li>
    {% else %}
      <li>暂无图表</li>
    {% endfor %}
  </ul>
  <script src="/static/upload.js"></script>
</body>
</html>
```

`templates/chart.html`:
```html
<!doctype html>
<html lang="zh">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ chart.title }} - PlotPin</title>
  <link rel="stylesheet" href="/static/app.css">
  <script src="/static/plotly.min.js"></script>
</head>
<body>
  <p><a href="/">← 返回</a></p>
  <div id="plot" style="width:100%;max-width:900px;height:520px;"></div>

  <div class="links">
    <p>交互页:<code id="link-page"></code></p>
    <p>PNG:<code id="link-png"></code></p>
    <p>SVG:<code id="link-svg"></code></p>
  </div>

  <script id="spec" type="application/json">{{ spec_json | safe }}</script>
  <script>
    const spec = JSON.parse(document.getElementById("spec").textContent);
    Plotly.newPlot("plot", spec.data, spec.layout, {responsive: true});
    const base = location.origin + "/chart/{{ chart.id }}";
    document.getElementById("link-page").textContent = base;
    document.getElementById("link-png").textContent = base + ".png";
    document.getElementById("link-svg").textContent = base + ".svg";
  </script>
</body>
</html>
```

`templates/404.html`:
```html
<!doctype html>
<html lang="zh">
<head><meta charset="utf-8"><title>未找到 - PlotPin</title>
<link rel="stylesheet" href="/static/app.css"></head>
<body><h1>404</h1><p>找不到该图表。<a href="/">返回首页</a></p></body>
</html>
```

- [ ] **Step 5: 写前端静态资源**

`static/app.css`:
```css
body { font-family: system-ui, sans-serif; max-width: 960px; margin: 2rem auto; padding: 0 1rem; }
.error { color: #b00020; font-weight: bold; }
#config { display: grid; gap: .5rem; margin-top: 1rem; }
#config label { display: block; }
.chart-list { line-height: 1.8; }
.links code { background: #f3f3f3; padding: .1rem .3rem; }
```

`static/upload.js`:
```javascript
// 选好文件后再展开配置区(纯客户端 UX,不改变提交逻辑)
const fileInput = document.getElementById("file");
const config = document.getElementById("config");
if (fileInput && config) {
  fileInput.addEventListener("change", () => {
    config.hidden = !fileInput.files.length;
  });
}
```

- [ ] **Step 6: 实现路由(重写 app/main.py)**

`app/main.py`:
```python
import json
import os

from fastapi import Depends, FastAPI, File, Form, Request, UploadFile
from fastapi.responses import HTMLResponse, RedirectResponse, Response
from fastapi.staticfiles import StaticFiles
from fastapi.templating import Jinja2Templates

from app.ids import new_id
from app.parsing import CSVParseError, read_csv_bytes
from app.rendering import build_plotly_spec, render_static
from app.storage import Storage

app = FastAPI(title="PlotPin")
app.mount("/static", StaticFiles(directory="static"), name="static")
templates = Jinja2Templates(directory="templates")

_storage: Storage | None = None


def get_storage() -> Storage:
    global _storage
    if _storage is None:
        _storage = Storage(os.environ.get("PLOTPIN_DATA_DIR", "data"))
    return _storage


@app.get("/healthz")
def healthz():
    return {"status": "ok"}


@app.get("/", response_class=HTMLResponse)
def index(request: Request, store: Storage = Depends(get_storage)):
    return templates.TemplateResponse(
        request, "index.html", {"charts": store.list_charts()}
    )


@app.post("/charts")
async def create_chart(
    request: Request,
    file: UploadFile = File(...),
    title: str = Form(...),
    x_title: str = Form(...),
    y_title: str = Form(...),
    x_eng: bool = Form(False),
    y_eng: bool = Form(False),
    store: Storage = Depends(get_storage),
):
    raw = await file.read()
    try:
        read_csv_bytes(raw)  # 仅校验;失败抛 CSVParseError
    except CSVParseError as err:
        return templates.TemplateResponse(
            request,
            "index.html",
            {"charts": store.list_charts(), "error": str(err)},
            status_code=400,
        )
    chart_id = new_id()
    while store.exists(chart_id):
        chart_id = new_id()
    store.save_chart(chart_id, title, x_title, y_title, x_eng, y_eng, raw)
    return RedirectResponse(url=f"/chart/{chart_id}", status_code=303)


@app.get("/chart/{chart_id}.png")
def chart_png(chart_id: str, store: Storage = Depends(get_storage)):
    return _image(chart_id, "png", "image/png", store)


@app.get("/chart/{chart_id}.svg")
def chart_svg(chart_id: str, store: Storage = Depends(get_storage)):
    return _image(chart_id, "svg", "image/svg+xml", store)


def _image(chart_id: str, ext: str, media_type: str, store: Storage) -> Response:
    chart = store.get_chart(chart_id)
    if chart is None:
        return _not_found_plain()
    cache = store.cache_path(chart_id, ext)
    if cache.exists():
        return Response(content=cache.read_bytes(), media_type=media_type)
    parsed = read_csv_bytes(store.read_csv(chart_id))
    data = render_static(
        parsed, chart.title, chart.x_title, chart.y_title,
        chart.x_eng, chart.y_eng, ext,
    )
    cache.write_bytes(data)
    return Response(content=data, media_type=media_type)


@app.get("/chart/{chart_id}", response_class=HTMLResponse)
def chart_page(chart_id: str, request: Request, store: Storage = Depends(get_storage)):
    chart = store.get_chart(chart_id)
    if chart is None:
        return templates.TemplateResponse(request, "404.html", {}, status_code=404)
    parsed = read_csv_bytes(store.read_csv(chart_id))
    spec = build_plotly_spec(
        parsed, chart.title, chart.x_title, chart.y_title,
        chart.x_eng, chart.y_eng,
    )
    return templates.TemplateResponse(
        request,
        "chart.html",
        {"chart": chart, "spec_json": json.dumps(spec)},
    )


def _not_found_plain() -> Response:
    return Response(content="404", media_type="text/plain", status_code=404)
```

注意:`get_storage` 用模块级单例;测试通过 `app.dependency_overrides[get_storage]` 注入临时目录的 `Storage`,所以单例不会污染测试。`/chart/{chart_id}.png` 路由声明在 `/chart/{chart_id}` 之前,确保带后缀的路径优先匹配。

- [ ] **Step 7: 运行测试确认通过**

Run: `pytest tests/test_routes.py -v`
Expected: PASS

- [ ] **Step 8: 运行全部测试**

Run: `pytest -v`
Expected: PASS(全部任务的测试)

- [ ] **Step 9: 手动冒烟(可选但建议)**

```bash
PLOTPIN_DATA_DIR=./data uvicorn app.main:app --reload
```
浏览器打开 `http://127.0.0.1:8000/`,上传一个示例 CSV,确认能生成图表页、PNG/SVG 链接可访问。

- [ ] **Step 10: 提交**

```bash
git add app/main.py templates static tests/test_routes.py
git commit -m "feat: routes, templates and frontend for upload and chart pages"
```

---

### Task 8: Docker 打包

**Files:**
- Create: `Dockerfile`, `docker-compose.yml`, `.dockerignore`

**Interfaces:**
- Consumes: 前述全部应用代码。
- Produces: 名为 `plotpin` 的 compose 服务,数据卷映射到 NAS 目录;容器内 `data/` 即 `PLOTPIN_DATA_DIR`。

- [ ] **Step 1: 写 .dockerignore**

`.dockerignore`:
```
.git
__pycache__
*.pyc
.pytest_cache
data
.venv
venv
docs
tests
```

- [ ] **Step 2: 写 Dockerfile**

`Dockerfile`:
```dockerfile
FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app ./app
COPY templates ./templates
COPY static ./static

ENV PLOTPIN_DATA_DIR=/data
EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

- [ ] **Step 3: 写 docker-compose.yml**

`docker-compose.yml`:
```yaml
services:
  plotpin:
    build: .
    container_name: plotpin
    ports:
      - "8000:8000"
    volumes:
      # 左侧改成 NAS 上的实际共享目录,确保持久化
      - ./data:/data
    environment:
      - PLOTPIN_DATA_DIR=/data
    restart: unless-stopped
```

- [ ] **Step 4: 构建并验证容器**

Run:
```bash
docker compose build
docker compose up -d
sleep 3
curl -fsS http://127.0.0.1:8000/healthz
```
Expected: 返回 `{"status":"ok"}`。

- [ ] **Step 5: 验证持久化(可选)**

上传一张图后 `docker compose down && docker compose up -d`,确认图表仍在(`data/` 卷未丢)。然后 `docker compose down`。

- [ ] **Step 6: 提交**

```bash
git add Dockerfile docker-compose.yml .dockerignore
git commit -m "chore: docker packaging with persistent data volume"
```

---

## Self-Review

**Spec coverage:**
- 上传 CSV + 第一列 X / 其余 Y + 表头作图例 → Task 4(解析)、Task 7(上传路由)。
- 配置三标题 + 两个工程计数法勾选 → Task 7(表单/模板)、Task 5(存储字段)。
- 折线图、同图多曲线、颜色+图例 → Task 6(matplotlib/Plotly)。
- 唯一永久 ID + 交互页 + `.png`/`.svg` → Task 3、Task 7。
- 工程计数法解析与显示、解析/显示分离、尾数区间、负号、大小写敏感 → Task 2,贯穿 4/6/7。
- 按需渲染 + 永久缓存 → Task 7 `_image`。
- 编码 UTF-8 + GBK 兜底 → Task 4。
- 严格校验(行列定位中文报错、空值视为错误) → Task 4、Task 7。
- 首页图表清单(倒序) → Task 5 `list_charts`、Task 7 `index`。
- 持久化卷、SQLite+文件系统 → Task 5、Task 8。
- 不可猜测短随机 ID(8–12 位) → Task 3。
- Plotly 本地打包(不走 CDN) → Task 7 Step 1 + 模板。
- Docker / docker-compose,服务名 plotpin,上传体积无人为收紧(默认放开) → Task 8。
- 明确不做项(检索/分组/编辑/删除/账号/其它图型) → 未实现,符合范围。

**Placeholder scan:** 无 TBD/TODO;每个代码步骤含完整代码与命令。

**Type consistency:** `ParsedCSV`(x_label/x/y_labels/ys)、`Chart`、`Storage` 方法名、`render_static`/`build_plotly_spec`/`build_plotly_spec` 返回结构、`get_storage` 在 Task 1 占位并在 Task 7 落地——前后一致。`x_eng`/`y_eng` 全程 `bool`,存库转 `int`。
